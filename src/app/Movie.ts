export interface Movie {
    id?: number,
    title: string, 
    year: string,
    runtime: string,
    genres: any,
    director: string,
    actors: string,
    plot: string,
    posterUrl: string;
}